
---
apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    run: query-url
  name: query-url
  namespace: query-url
spec:
  replicas: 1
  selector:
    matchLabels:
      run: query-url
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: query-url
    spec:
      containers:
      - image: docker.pkg.github.com/rekhaeshwar/python-prometheus/query-url:0.0.1
        name: query-url
        resources: {}
        resources:
          requests:
            memory: "100Mi"
            cpu: "200m"
          limits:
            memory: "500Mi"
            cpu: "1000m"
        volumeMounts:
        - name: query-url-config
          mountPath: /app/config
        readinessProbe:
          httpGet:
            path: /healthcheck
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 30
          timeoutSeconds: 10
      imagePullSecrets:
      - name: git-reg-cred
      volumes:
      - name: query-url-config
        configMap:
          name: query-url-config
